# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOSFrameProject' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOSFrameProject
    pod 'AFNetworking'
    pod 'SDWebImage', '~>3.8'
    pod 'pop', '~> 1.0'
    pod 'Masonry'
    pod 'SwiftyJSONMappable'

    # 高德地图集成
 #   pod 'AMap3DMap'  #3D地图与2D地图不能同时使用
 #   pod 'AMapLocation'   #定位SDK
 #   pod 'AMapSearch'     #搜索功能

  target 'iOSFrameProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOSFrameProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
